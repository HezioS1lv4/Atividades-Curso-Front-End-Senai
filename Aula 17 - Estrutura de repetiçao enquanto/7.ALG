algoritmo "semnome"

var
 i, soma, num: inteiro
inicio
 soma <- 0
 para i de 1 ate 10 faca
 escreval("Digite um numero")
 leia(num)
 soma <- soma + num
 fimpara
 escreva("O valor da soma é: ", soma)
fimalgoritmo